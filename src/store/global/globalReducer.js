
const defaultState = {
    application:'Learn React, Redux and Router',
    author:'rainbowx',
    website:'http://github.com/juliarainbowx'
}

const reducer = (state = defaultState, action) => {

    return state

}

export default reducer