import React from 'react'

const About = (props) => {
    return (
        <section>
            <h1>About</h1>
            <p>A simple application (Counter & To-Do List) with <code>redux</code> and <code>react-router</code>.</p>
        </section>
    )
}

export default About